{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to .NET Core (Hot Reload)",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "justMyCode": true
        }
    ]
}