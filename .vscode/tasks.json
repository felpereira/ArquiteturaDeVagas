{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "watch",
            "type": "shell",
            "command": "dotnet watch run",
            "options": {
                "cwd": "${workspaceFolder}/ArquiteturaDeVagas.API"
            },
            "problemMatcher": "$msCompile"
        }
    ]
}